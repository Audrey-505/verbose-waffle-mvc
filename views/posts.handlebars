{{#if logged_in}}
<div class="text-center">
  <h2>{{title}}</h2>
  <p>Created by {{user.email}} OR {{id}} on {{date}}</p>
  <p>
    {{post}}
{{#each comments}}
    <p>Comments: {{comment_text}} </p>
    <p>{{created_at}}</p>
    {{/each}}
  </p>
  <div class="update-post"><a href="/editpost/{{id}}"><button class="btn btn-sm btn-danger" data-id="{{id}}">UPDATE</button></a></div>
</div>
{{else}}
<div class="text-center">
  <h2>{{title}}</h2>
  <p>Created by {{user.email}} OR {{id}} on {{date}}</p>
  <p>
    {{post}}
  </p>
</div>
{{/if}}